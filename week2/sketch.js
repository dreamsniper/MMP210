function setup() {
createCanvas(420, 480);
var x = 50;
var y = 100;
var z = 0;
var increment = 10;	
var red =  color((y*2)+5, z, z);
var green = color(z, (y*2)+4, z);
var yellow = color((y*2)+5,(y*2)+5, z);
var blue = color(z, y+2,(y*2)+5);
var violet = color((y*2)+4, x+1, (y*2)+55)
var black = color(0,0,0);

background(black);
noStroke();

fill(red);
ellipse(x*2,x*2,x,x);

fill(green);
rect(y*2,y*2,y,y);

fill(yellow);
triangle(x,y*3,y,x*5,75,y*2);

fill(blue);
ellipse(y*3,y, y,x*3);

fill(violet);
rect(y,y,x*3,x*3)

x+= increment;
++x;
y+= increment;
z++;
z+= increment;

fill(red);
ellipse(x*2,x*2,x,x);

fill(green);
rect(y*2,y*2,y,y);

fill(yellow);
triangle(x,y*3,y,x*5,75,y*2);

fill(blue);
ellipse(y*3,y, y,x*3);

fill(violet);
rect(y,y,x*3,x*3)

x+= increment;
++x;
y+= increment;
z++;
z+= increment;

fill(red);
ellipse(x*2,x*2,x,x);

fill(green);
rect(y*2,y*2,y,y);

fill(yellow);
triangle(x,y*3,y,x*5,75,y*2);

fill(blue);
ellipse(y*3,y, y,x*3);

fill(violet);
rect(y,y,x*3,x*3);

x+= increment;
++x;
y+= increment;
z++;
z+= increment;

fill(red);
ellipse(x*2,x*2,x,x);

fill(green);
rect(y*2,y*2,y,y);

fill(yellow);
triangle(x,y*3,y,x*5,75,y*2);

fill(blue);
ellipse(y*3,y, y,x*3);

fill(violet);
rect(y,y,x*3,x*3);

y-= increment;
x++;
x-= increment;
z+= increment;

fill(red);
ellipse(x*2,x*2,x,x);

fill(green);
rect(y*2,y*2,y,y);

fill(yellow);
triangle(x,y*3,y,x*5,75,y*2);

fill(blue);
ellipse(y*3,y, y,x*3);

fill(violet);
rect(y,y,x*3,x*3);

y-= increment;
x++;
x-= increment;
z+= increment;

fill(yellow);
ellipse(x*2,x*2,x,x);

fill(green);
rect(y*2,y*2,y,y);

fill(red);
triangle(x,y*3,y,x*5,75,y*2);

fill(violet);
ellipse(y*3,y, y,x*3);

fill(blue);
rect(y,y,x*3,x*3);

y-= increment;
x++;
x-= increment;
z+= increment;

fill(yellow);
ellipse(x*2,x*2,x,x);

fill(green);
rect(y*2,y*2,y,y);

fill(red);
triangle(x,y*3,y,x*5,75,y*2);

fill(violet);
ellipse(y*3,y, y,x*3);

fill(blue);
rect(y,y,x*3,x*3);

//______________________________________________

var z = 0;
increment = 20

fill(z, z, z);
ellipse(z, increment, increment*2, increment*2);

z += increment;
fill(z, z, z);
ellipse(z, increment, increment*2, increment*2);

z += increment;
fill(z, z, z);
ellipse(z, increment, increment*2, increment*2);

z += increment;
fill(z, z, z);
ellipse(z, increment, increment*2, increment*2);

fill(z, z, z);
ellipse(z, increment, increment*2, increment*2);

z += increment;
fill(z, z, z);
ellipse(z, increment, increment*2, increment*2);

z += increment;
fill(z, z, z);
ellipse(z, increment, increment*2, increment*2);

z += increment;
fill(z, z, z);
ellipse(z, increment, increment*2, increment*2);

fill(z, z, z);
ellipse(z, increment, increment*2, increment*2);

z += increment;
fill(z, z, z);
ellipse(z, increment, increment*2, increment*2);

z += increment;
fill(z, z, z);
ellipse(z, increment, increment*2, increment*2);

z += increment;
fill(z, z, z);
ellipse(z, increment, increment-x, increment*2);

z += increment;
fill(z, z, z);
ellipse(z, increment, increment-x, increment*2);

z += increment;
fill(z, z, z);
ellipse(z, increment, increment-x, increment*2);

z += increment;
fill(z, z, z);
ellipse(z, increment, increment-x, increment*2);

z += increment;
fill(z, z, z);
ellipse(z, increment, increment-x, increment*2);

z += increment;
fill(z, z, z);
ellipse(z, increment, increment-x, increment*2);

z += increment;
fill(z, z, z);
ellipse(z, increment, increment-x, increment*2);

z += increment;
fill(z, z, z);
ellipse(z, increment, increment-x, increment*2);

z += increment;
fill(z, z, z);
ellipse(z, increment, increment-x, increment*2);

z += increment;
fill(z, z, z);
ellipse(z, increment, increment-x, increment*2);

z += increment;
fill(z, z, z);
ellipse(z, increment, increment-x, increment*2);

z += increment;
fill(z, z, z);
ellipse(z, increment, increment-x, increment*2);

z += increment;
fill(z, z, z);
ellipse(z, increment, increment-x, increment*2);

z += increment;
fill(z, z, z);
ellipse(z, increment, increment-x, increment*2);

//__________________________________________________

increment = 20;
z = 0;

fill(z,0,0);
rect(z,400,100,100);
z += increment;
y += increment;

fill(z,0,0);
rect(z,400,100,100);
z += increment;
y += increment;

fill(z,0,0);
rect(z,400,100,100);
z += increment;
y += increment;

fill(z,0,0);
rect(z,400,100,100);
z += increment;
y += increment;

fill(z,0,0);
rect(z,400,100,100);
z += increment;
y += increment;

fill(z,0,0);
rect(z,400,100,100);
z += increment;
y += increment;

fill(z,0,0);
rect(z,400,100,100);
z += increment;
y += increment;

fill(z,0,0);
rect(z,400,100,100);
z += increment;
y += increment;

fill(z,0,0);
rect(z,400,100,100);
z += increment;
y += increment;

fill(z,0,0);
rect(z,400,100,100);
z += increment;
y += increment;

fill(z,0,0);
rect(z,400,100,100);
z += increment;
y += increment;

fill(z,0,0);
rect(z,400,100,100);
z += increment;
y += increment;

fill(z,0,0);
rect(z,400,100,100);
z += increment;
y += increment;

fill(z,0,0);
rect(z,400,100,100);
z += increment;
y += increment;

fill(z,0,0);
rect(z,400,100,100);
z += increment;
y += increment;

fill(z,0,0);
rect(z,400,100,100);
z += increment;
y += increment;

fill(z,0,0);
rect(z,400,100,100);
z += increment;
y += increment;

//___________________________________________
var z = 100;
increment = 20

fill(z, z, z);
ellipse(z, y-y, increment*2, increment*2);

y += increment;
z += increment;
fill(z, z, z);
ellipse(z, y-y, increment*2, increment*2);

y += increment;
z += increment;
fill(z, z, z);
ellipse(z, y-y, increment*2, increment*2);

y += increment;
z += increment;
fill(z, z, z);
ellipse(z, y-y, increment*2, increment*2);

y += increment;
z += increment;
fill(z, z, z);
ellipse(z, y-y, increment*2, increment*2);

y += increment;
z += increment;
fill(z, z, z);
ellipse(z, y-y, increment*2, increment*2);

fill(z, z, z);
ellipse(z, y-y, increment*2, increment*2);

y += increment;
z += increment;
fill(z, z, z);
ellipse(z, y-y, increment*2, increment*2);

y += increment;
z += increment;
fill(z, z, z);
ellipse(z, y-y, increment*2, increment*2);

y += increment;
z += increment;
fill(z, z, z);
ellipse(z, y-y, increment*2, increment*2);

y += increment;
z += increment;
fill(z, z, z);
ellipse(z, y-y, increment*2, increment*2);

y += increment;
z += increment;
fill(z, z, z);
ellipse(z, y-y, increment*2, increment*2);

y += increment;
z += increment;
fill(z, z, z);
ellipse(z, y-y, increment*2, increment*2);

y += increment;
z += increment;
fill(z, z, z);
ellipse(z, y-y, increment*2, increment*2);

y += increment;
z += increment;
fill(z, z, z);
ellipse(z, y-y, increment*2, increment*2);

y += increment;
z += increment;
fill(z, z, z);
ellipse(z, y-y, increment*2, increment*2);

y += increment;
z += increment;
fill(z, z, z);
ellipse(z, y-y, increment*2, increment*2);
    

}

